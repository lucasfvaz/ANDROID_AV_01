<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <item
        android:id="@+id/mi_adicionar"
        android:title="Adicionar"
        app:showAsAction="always"
        android:icon="@drawable/ic_adicionar"
        android:onClick="seanunciar"/>
    <item
        android:id="@+id/mi_remover"
        android:title="Remover"
        android:onClick="remover"
        app:showAsAction="always"
        android:icon="@drawable/ic_remover"/>

    <item
        android:id="@+id/mi_editar"
        android:title="Editar"
        android:onClick="editar"
        app:showAsAction="always"
        android:icon="@drawable/ic_edit"/>

</menu>